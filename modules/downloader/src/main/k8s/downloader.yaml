apiVersion: apps/v1
kind: Deployment
metadata:
  name: downloader
spec:
  selector:
    matchLabels:
      app: downloader
  strategy:
    type: Recreate
  template:
    metadata:
      labels:
        app: downloader
    spec:
      containers:
        - name: downloader
          image: @docker.registry.uri@/@docker.image.name@
          imagePullPolicy: Always
          volumeMounts:
            - mountPath: /data/video/downloads
              name: downloads
            - mountPath: /var/log/downloader
              name: logs
      volumes:
        - name: downloads
          azureFile:
            secretName: storage-secret
            shareName: youtube-trimmer/video/downloads
            readOnly: false
        - name: logs
          azureFile:
            secretName: storage-secret
            shareName: youtube-trimmer/logs/downloader
            readOnly: false