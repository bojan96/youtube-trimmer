spring:
  datasource:
    url: jdbc:mysql://localhost/youtube_trimmer?user=root&password=root

authentication:
  token:
    signing-key-path: ${user.home}/youtube-trimmer/secret/key
    expiration-duration: 7   # Days

storage:
  video-container-name: trimmed
  connection-string-path: ${user.home}/youtube-trimmer/secret/azurestorageconnectionstring
  sas-token-expiry-duration-days: 2
  download-url-base: http://127.0.0.1:10000/devstoreaccount1
---
spring:
  config:
    activate:
      on-profile: k8s
  datasource:
    url: jdbc:mysql://mysql/youtube_trimmer?user=root&password=root
  rabbitmq:
    host: rabbitmq

authentication:
  token:
    signing-key-path: /app/secret/key

storage:
  connection-string-path: /secret/azurestorageconnectionstring
  download-url-base: https://youtubetrimmer.blob.core.windows.net
